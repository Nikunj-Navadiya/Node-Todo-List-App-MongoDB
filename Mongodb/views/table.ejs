<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Record</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<style>
    header {
        padding: 15px;
        background-color: antiquewhite;
        font-family: 'Courier New', Courier, monospace;
    }

    .btn a {
        color: #fff;
        text-decoration: none;
    }
    .card-body{
        background-color: aliceblue;
    }
    .card:hover{
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.3);
    }
</style>

<body>

    <header>
        <div class="header d-flex justify-content-between align-items-center">
            <div class="text">
                <h1>A Book shop</h1>
            </div>
            <div class="btn btn-primary">
                <a href="/add">Add New Record</a>
            </div>
        </div>
    </header>

    <div class="container mt-4">
        <div class="row">
            <% users.map((val, index)=> { %>
                <div class="col-md-4 mb-3">
                    <div class="card">
                        <img src="<%= val.image %>" class="card-img-top" alt="<%= val.name %>"
                            style="height: 250px; object-fit: contain;">
                        <div class="card-body">
                            <h5 class="card-title"><strong>Book Name : </strong>
                                <%= val.name %>
                            </h5>
                            <p class="card-text mb-2"><strong>Price : </strong>
                                <%= val.price %>
                            </p>
                            <p class="card-text mb-2"><strong>Pages : </strong>
                                <%= val.pages %>
                            </p>
                            <p class="card-text mb-2"><strong>Author : </strong>
                                <%= val.author %>
                            </p>
                            <a href="/editRecord?editId=<%= val.id %>" class="btn btn-primary">Edit</a>
                            <a href="/deleteRecord?deleteId=<%= val.id %>" class="btn btn-danger">Delete</a>
                        </div>
                    </div>
                </div>
                <% }) %>
        </div>

    </div>

    <!-- Bootstrap JS (Optional, for interactivity) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>